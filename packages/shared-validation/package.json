{
  "name": "shared-validation",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Zod validation schemas used across apps/client, apps/admin, and apps/server.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepare": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
