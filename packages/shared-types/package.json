{
  "name": "shared-types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript interfaces used across apps/client, apps/admin, and apps/server.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepare": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
