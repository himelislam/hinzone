// Relative to VITE_API_URL - see constants/auth-endpoints.constants.ts for the
// same convention.
export const USERS_ENDPOINTS = {
  PROFILE: '/users/profile',
  PROFILE_IMAGE: '/users/profile/image',
} as const;
